[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "printguard-shared"
version = "0.1.0"
dependencies = ["cryptography>=41.0.0"]

[project.optional-dependencies]
dev = ["pytest"]

